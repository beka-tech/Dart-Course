//we don't tell dart a Datatype  But some time it get harder for dart to know the datatype
//Dart infer datatype automatically infere it own
void main(List<String> args) {
  const int herage = 20;
  const myage = 24;
  print(herage);
  print(myage);
}